.claude/*
.gitignore
docs/

# Private reference material — NEVER deploy publicly.
# The master CV is for local coding-agent reference only.
local_refs/
master_cv.pdf
one_page_resume.pdf

# Camera raw files — keep local, never deploy
*.dng
*.DNG
*.raw
*.RAW
*.arw
*.cr2
*.nef
data/photos.json.bak
